// src/app/(tabs)/impacto.tsx
import { Text, View } from 'react-native';

// TEMPORARY — real Mi Impacto screen comes in a later part.
export default function ImpactoPlaceholder() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Impacto — coming soon</Text>
    </View>
  );
}