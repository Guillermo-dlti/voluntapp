import { Text, View } from 'react-native';

// TEMPORARY — replaced by the real Home Hub screen once the tab bar is built.
export default function TempTabsPlaceholder() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Tabs coming soon</Text>
    </View>
  );
}