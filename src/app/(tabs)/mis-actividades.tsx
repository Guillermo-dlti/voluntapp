// src/app/(tabs)/mis-actividades.tsx
import { Text, View } from 'react-native';

// TEMPORARY — real Mis Actividades screen comes in a later part.
export default function MisActividadesPlaceholder() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Mis Actividades — coming soon</Text>
    </View>
  );
}